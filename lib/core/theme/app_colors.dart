import 'package:flutter/material.dart';

class AppColors {
  // Colores Primarios del Manual (Pág 20)
  static const Color primaryGreen = Color(0xFF6FB634); // El verde oficial
  static const Color darkBlue = Color(0xFF0E1428); // El azul oscuro oficial
  static const Color bgColor =
      darkBlue; // Esto arregla todos los errores de una vez

  // Colores de Apoyo
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color greyLight = Color(0xFFF5F5F5);
  static const Color greyMedium = Color(0xFFB0BEC5);

  // Semántica
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);
}
